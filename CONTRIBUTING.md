# Contributing

Contributions are welcome after the v1 configuration and plugin contracts are
stabilized. Before submitting a change, run:

```powershell
python -m pytest
ruff check .
ruff format --check .
```

Keep changes focused, include tests for behavior changes, and update the relevant
documentation.

